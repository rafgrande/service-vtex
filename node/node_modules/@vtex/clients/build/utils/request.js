"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRequestConfig = void 0;
const authToken_1 = require("./authToken");
const tracing_1 = require("./tracing");
const getRequestConfig = (context, authMethod, metric, tracingConfig
// eslint-disable-next-line max-params
) => {
    const token = (0, authToken_1.getAuthToken)(context, authMethod);
    const headers = token
        ? {
            VtexIdclientAutCookie: token,
        }
        : {};
    return {
        headers,
        metric,
        tracing: (0, tracing_1.createTracing)(metric, tracingConfig),
    };
};
exports.getRequestConfig = getRequestConfig;
